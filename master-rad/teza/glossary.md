# Glossary (EN → SR)

Consistent Serbian terminology for the translation pass. `keep` means the English term is the
standard in Serbian CS writing and should stay in English (optionally glossed on first use).
Fill/adjust as we translate; this is the single source of truth for terms.

| English | Serbian (preferred) | Note |
|---|---|---|
| ray tracing | trasiranje zrakova / ray tracing (`keep`) | gloss once, then `ray tracing` |
| rasterization | rasterizacija | |
| hybrid rendering | hibridno renderovanje | |
| inline ray tracing / ray query | inline ray query (`keep`) | `VK_KHR_ray_query` |
| ray tracing pipeline | ray tracing pipeline (`keep`) | vs ray query |
| shader binding table (SBT) | SBT (`keep`) | |
| acceleration structure | struktura ubrzanja | akceleraciona struktura also ok |
| bounding volume hierarchy (BVH) | BVH (`keep`) | hijerarhija graničnih zapremina |
| TLAS / BLAS | TLAS / BLAS (`keep`) | top/bottom-level accel structure |
| global illumination (GI) | globalno osvetljenje | |
| ambient occlusion (AO) | ambijentalna okluzija | |
| reflections | refleksije | |
| shadows | senke | |
| soft shadows | meke senke | |
| denoising | uklanjanje šuma / denoising (`keep`) | |
| SVGF | SVGF (`keep`) | spatiotemporal variance-guided filtering |
| à-trous wavelet filter | à-trous filter | |
| temporal accumulation | temporalna akumulacija | |
| temporal anti-aliasing (TAA) | TAA (`keep`) | |
| reprojection | reprojekcija | |
| disocclusion | disokluzija | |
| G-buffer | G-bafer | |
| forward rendering | forward (direktno) renderovanje | |
| depth prepass / early-Z | depth prepass / rani-Z | |
| motion vectors | vektori kretanja | |
| jitter | džiter / pomeraj poduzorka | |
| render graph | graf renderovanja | |
| render pass | prolaz (renderovanja) | |
| compute shader | compute šejder (`keep` compute) | |
| samples per pixel (spp) | uzoraka po pikselu | |
| frame time | vreme frejma / vreme kadra | |
| variance | varijansa | |
| irradiance / radiance | iradijansa / radijansa | |
| path tracing | path tracing (`keep`) | referentni put |
| ground truth | referentni izlaz / ground truth (`keep`) | |
| PSNR / SSIM / LPIPS | PSNR / SSIM / LPIPS (`keep`) | metrike kvaliteta |
| screen-space | screen-space (`keep`) | u prostoru ekrana |
| probe / irradiance probe | proba iradijanse | DDGI |
| reservoir sampling | reservoir sampling (`keep`) | ReSTIR |
| occupancy | zauzetost (occupancy) | RGA/GPU |
| entity-component-system (ECS) | ECS (`keep`) | |
