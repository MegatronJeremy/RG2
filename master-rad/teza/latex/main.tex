% !TeX program = pdflatex
% Master rad, ETF Beograd, using the official etf.cls (master option).
% Drafting language: English (translate to Serbian later); structural strings are Serbian via babel.
% Build: pdflatex -> bibtex -> pdflatex -> pdflatex  (see Scripts note / latexmk).
\documentclass[master,a4paper,12pt,oneside]{etf}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage[a4paper, top=30mm, bottom=30mm, left=20mm, right=20mm]{geometry} % PPK margins
\usepackage[serbian]{babel}

\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{xcolor}
\usepackage{listings}
\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning, fit, backgrounds}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepackage{microtype}
\usepackage[hidelinks]{hyperref}
\usepackage{underscore} % literal _ in text/\texttt, subscript still works in math

% Serbian name for the abstract heading (class default is English "Abstract").
\addto\captionsserbian{\renewcommand{\abstractname}{Sa\v{z}etak}}

% Inline/code listing defaults (characteristic snippets, per PPK; not full code).
\lstset{basicstyle=\ttfamily\small, breaklines=true, columns=fullflexible,
        frame=single, captionpos=b, tabsize=2, showstringspaces=false}

% Working title (SR). TODO: finalize; keep in sync with 00-naslovna.md.
\title{Hibridno hardversko pra\'cenje zrakova u realnom vremenu kroz Vulkan: cross-vendor pore\dj enje na AMD i NVIDIA GPU-ovima}
\author{Vuk \DJ or\dj evi\'c}
\indeks{2024/3102}
\mentor{prof. (TBD)}
\date{2026}

\begin{document}

\maketitle

% --- Sa\v{z}etak + klju\v{c}ne re\v{c}i (PPK). English draft; numbers TODO from Ch4. ---
\begin{abstract}
This thesis integrates four ray-traced lighting effects (shadows, ambient occlusion, reflections,
and diffuse global illumination) as inline ray-query (\texttt{VK_KHR_ray_query}) passes over a
rasterized forward renderer in a custom Vulkan engine, denoised by a shared SVGF filter. Real-time
rasterization approximates these global effects with screen-space and precomputed methods that fail
on off-screen or dynamic information; hardware ray tracing makes per-pixel scene queries feasible.
The per-effect GPU cost is isolated with an A/B configuration matrix, and reconstruction quality is
measured with PSNR/SSIM against a supersampled reference on a fixed scene and camera. The identical
pipeline is compared across two vendors, AMD RDNA4 and NVIDIA Blackwell, isolating the effect of
hardware and driver on the same code. Main numerical results: TODO (fill from Chapter 4).
\end{abstract}

\begin{keywords}
ray tracing u realnom vremenu; ray query; Vulkan; globalno osvetljenje; ambijentalna okluzija;
refleksije; SVGF; uklanjanje \v{s}uma; hibridno renderovanje; cross-vendor; AMD RDNA4; NVIDIA Blackwell
\end{keywords}

\tableofcontents
\listoffigures
\listoftables

\input{chapters/01-uvod}
\input{chapters/02-pregled}
\input{chapters/03-implementacija}
\input{chapters/04-analiza}
\input{chapters/05-zakljucak}

\bibliographystyle{unsrt} % PPK: references ordered by citation order
\bibliography{references}

\end{document}
