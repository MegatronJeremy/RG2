\chapter{Review of existing solutions and technologies}
\label{ch:pregled}


\section{Rasterization versus ray tracing}
\label{sec:rastervsrt}

Rasterization projects each triangle onto the image plane and resolves visibility with a depth
buffer: a stream operation that maps well to GPU hardware, although it processes one triangle at a
time with no access to the rest of the scene, so it cannot natively answer ``what else is visible
from this point.'' Ray tracing instead casts rays into the scene, finds their nearest intersections
\cite{moller1997raytri}, and estimates light transport by sampling \cite{pharr2016pbrt}, which
answers arbitrary point-to-point visibility (what indirect lighting, reflections, and occlusion
require) at the cost of per-ray traversal work and sampling noise. The common reference for both is
the rendering equation \cite{kajiya1986rendering}, splitting outgoing radiance $L_o$ (light energy
per unit projected area per unit solid angle, the physical quantity a pixel ultimately stores) at a
point $\mathbf{x}$ in direction $\omega_o$ into emitted radiance plus the reflected integral over the
hemisphere $\Omega$:
\begin{equation}
L_o(\mathbf{x}, \omega_o) = L_e(\mathbf{x}, \omega_o) + \int_{\Omega}
f_r(\mathbf{x}, \omega_i, \omega_o)\, L_i(\mathbf{x}, \omega_i)\, (\omega_i \cdot \mathbf{n})\,
\mathrm{d}\omega_i,
\label{eq:rendering}
\end{equation}
where $f_r$ is the BRDF, $L_i$ the incident radiance, and $\mathbf{n}$ the surface normal.
Rasterization resolves the emitted and direct terms efficiently through projection, but the integral
needs incident radiance from every direction and therefore scene-wide visibility, which is exactly
what it cannot express and what ray tracing evaluates by sampling. A hybrid renderer uses each where
it is strong (Figure~\ref{fig:rastervsrt}) \cite{akenine2018rtr}.

\begin{figure}[tbp]
\centering
\begin{tikzpicture}[>={Stealth[]}, scale=0.9]
  \begin{scope}
    \node[font=\small] at (1.9,3.1) {Rasterization};
    \fill (0,1.3) circle (1.6pt) node[left, font=\scriptsize] {eye};
    \draw[thick] (1,0.35) -- (1,2.35); \node[font=\scriptsize] at (1,0.05) {image plane};
    \draw[fill=black!8] (3.4,0.7) -- (4.0,2.1) -- (2.8,1.95) -- cycle;
    \draw[black!55] (0,1.3) -- (3.4,0.7);
    \draw[black!55] (0,1.3) -- (4.0,2.1);
    \draw[black!55] (0,1.3) -- (2.8,1.95);
    \node[font=\scriptsize] at (3.5,2.4) {triangle};
  \end{scope}
  \begin{scope}[xshift=6.4cm]
    \node[font=\small] at (1.9,3.1) {Ray tracing};
    \fill (0,1.3) circle (1.6pt) node[left, font=\scriptsize] {eye};
    \draw[thick] (1,0.35) -- (1,2.35); \node[font=\scriptsize] at (1,0.05) {image plane};
    \fill[black!8] (3.5,1.3) circle (0.5);
    \draw[->, black!70] (0,1.3) -- (2.95,1.3);
    \node[font=\scriptsize] at (2.0,1.55) {ray};
  \end{scope}
\end{tikzpicture}
\caption[Rasterization versus ray tracing]{Rasterization (projecting triangles onto the image plane) versus ray tracing (a ray through
a pixel into the scene to the nearest intersection).}
\label{fig:rastervsrt}
\end{figure}

\section{Acceleration structures: BVH, TLAS/BLAS}
\label{sec:accel-pregled}

Testing every ray against every triangle is $O(\text{rays} \times \text{triangles})$ and hopeless at
scene scale, so ray tracing depends on a spatial acceleration structure. The standard one is a
bounding-volume hierarchy (BVH): a tree of nested bounding boxes whose traversal descends only into
boxes a ray actually enters, reducing the expected cost per ray to roughly logarithmic in the
triangle count (Figure~\ref{fig:bvh}).

\begin{figure}[tbp]
\centering
\begin{tikzpicture}[>={Stealth[]}, font=\small,
  tri/.style={draw, fill=black!12},
  bx/.style={draw, rounded corners=1pt, thick},
  nd/.style={draw, rounded corners, minimum width=7mm, minimum height=5mm, font=\scriptsize,
             inner sep=2pt},
  hit/.style={nd, fill=black!12},
  skip/.style={nd, densely dashed, text=black!45, draw=black!45}]

  \begin{scope}
    \node[font=\small] at (2.5,4.0) {the scene, boxed};
    \draw[bx, black!45] (0.15,0.15) rectangle (4.85,3.5);
    \node[font=\scriptsize, black!45] at (4.62,3.28) {A};
    \draw[bx] (0.35,0.4) rectangle (2.3,3.05);  \node[font=\scriptsize] at (0.6,2.82) {B};
    \draw[bx] (2.6,0.4) rectangle (4.65,3.05);  \node[font=\scriptsize] at (2.85,2.82) {C};
    \draw[tri] (0.7,0.8) -- (1.5,0.8) -- (1.1,1.6) -- cycle;
    \draw[tri] (1.2,1.9) -- (2.05,1.9) -- (1.6,2.7) -- cycle;
    \draw[tri] (2.9,0.8) -- (3.7,0.8) -- (3.3,1.6) -- cycle;
    \draw[tri] (3.6,1.9) -- (4.4,1.9) -- (4.0,2.7) -- cycle;
    % the ray enters A and B, misses C entirely, so C's whole subtree is never opened
    \draw[->, very thick] (-0.5,1.15) -- (2.35,1.15);
    \node[font=\scriptsize] at (-0.1,1.45) {ray};
  \end{scope}

  \begin{scope}[xshift=7.2cm, yshift=0.35cm]
    \node[font=\small] at (1.6,3.65) {the tree it walks};
    \node[hit]  (a)  at (1.6,3.0) {A};
    \node[hit]  (b)  at (0.6,2.0) {B};
    \node[skip] (c)  at (2.6,2.0) {C};
    \node[hit]  (t1) at (0.1,1.0) {$\triangle$};
    \node[skip] (t2) at (1.1,1.0) {$\triangle$};
    \node[skip] (t3) at (2.1,1.0) {$\triangle$};
    \node[skip] (t4) at (3.1,1.0) {$\triangle$};
    \draw (a) -- (b); \draw[black!45] (a) -- (c);
    \draw (b) -- (t1); \draw[black!45] (b) -- (t2);
    \draw[black!45] (c) -- (t3); \draw[black!45] (c) -- (t4);
    \node[font=\scriptsize, align=left] at (1.6,0.25)
      {shaded = box entered, tested\\dashed = never opened};
  \end{scope}
\end{tikzpicture}
\caption[How a bounding-volume hierarchy saves work]{A bounding-volume hierarchy on four triangles.
Each box encloses everything below it, so a ray that misses a box cannot hit anything inside it and
the whole subtree is skipped without a single triangle test. Here the ray enters A and B and misses
C, so it tests one triangle instead of four. Real hierarchies are deeper and wider, which is what
turns this into roughly logarithmic rather than linear cost, and the two GPUs differ in who walks the
tree rather than in the shape of it (Figure~\ref{fig:traversal}).}
\label{fig:bvh}
\end{figure} How well a given tree performs depends on where the splits are placed, chosen by a cost heuristic
(most commonly the surface area heuristic, which favours splits that leave a ray unlikely to enter
both children). On this hardware the driver builds the tree and picks that heuristic, so it is not a
parameter this work sets. On current GPUs the ray-box and ray-triangle
tests are fixed-function hardware, which is what makes a ray per pixel affordable in real time. How
much of the \emph{traversal} around those tests is also fixed-function differs by vendor, and the two
GPUs evaluated in Chapter~\ref{ch:analiza} sit on opposite sides of that line
(Figure~\ref{fig:traversal}): NVIDIA's RT cores walk the hierarchy in hardware \cite{nvidia_turing},
while on AMD's RDNA architectures the traversal loop is driven by shader instructions that issue
hardware intersection tests \cite{amd_rdna_isa}, so traversal competes with shading for the same
execution resources. That is a structural reason to \emph{expect} the same shader to cost differently
on the two vendors, not an attribution. Section~\ref{sec:cena} measures a per-pass AMD/NVIDIA cost
ratio spanning \perfRatioMin{} to \perfRatioMax{} in the \texttt{+gi} configuration, so the
difference is not one global
clock factor. This evaluation does not isolate how much of that gap comes from this mechanism rather
than from clocks, cache, or driver.

\begin{figure}[tbp]
\centering
\begin{tikzpicture}[>={Stealth[]}, font=\small,
  unit/.style={draw, rounded corners, align=center, minimum width=2.6cm, minimum height=9mm},
  fixed/.style={unit, fill=black!8}]
  \begin{scope}
    \node[font=\small\bfseries] at (1.9,3.5) {NVIDIA (RT cores)};
    \node[unit] (sm) at (1.9,2.5) {SM / shader};
    \node[fixed, below=12mm of sm, minimum height=14mm, text width=2.4cm] (rtc)
      {RT core:\\traversal \emph{and} intersections};
    \draw[->] ([xshift=-9mm]sm.south) -- node[left, font=\scriptsize] {1 ray} ([xshift=-9mm]rtc.north);
    \draw[<-] ([xshift=9mm]sm.south) -- node[right, font=\scriptsize] {hit} ([xshift=9mm]rtc.north);
  \end{scope}
  \begin{scope}[xshift=6.6cm]
    \node[font=\small\bfseries] at (1.9,3.5) {AMD RDNA (ray query)};
    \node[unit, text width=2.4cm] (cu) at (1.9,2.5) {CU / shader:\\traversal loop};
    \node[fixed, below=12mm of cu, text width=2.4cm] (isect) {intersection unit};
    \draw[->] ([xshift=-9mm]cu.south) -- node[left, font=\scriptsize] {per node} ([xshift=-9mm]isect.north);
    \draw[<-] ([xshift=9mm]cu.south) -- node[right, font=\scriptsize] {result} ([xshift=9mm]isect.north);
  \end{scope}
\end{tikzpicture}
\caption[Where the traversal loop lives]{Ray-box and ray-triangle intersections are fixed-function on
both architectures, and what differs is who drives the traversal loop. On NVIDIA the loop is in
hardware too, so the shader hands over a ray and waits for a hit, while on RDNA architectures the loop
runs in the shader, which calls the intersection unit per visited node, so traversal consumes the same
execution resources as shading. The diagram explains why a cost difference is expected, not how large
it is.}
\label{fig:traversal}
\end{figure}

Vulkan exposes the BVH as a two-level structure: a bottom-level acceleration structure
(BLAS) holds a mesh's triangles in object space, and a top-level one (TLAS) holds instances, each a
transform referencing a BLAS. The split lets many instances share one BLAS and lets the TLAS, which
changes as objects move, be rebuilt per frame while the heavier BLAS persists
(Figure~\ref{fig:tlasblas}). Either can be fully rebuilt (highest traversal quality) or refit (box
extents updated for small deformations, cheaper but lower quality). Dynamic scenes trade these off
each frame.

\begin{figure}[tbp]
\centering
\begin{tikzpicture}[>={Stealth[]},
  inst/.style={draw, rounded corners, font=\small, align=center, minimum width=2.9cm, minimum height=9mm},
  blas/.style={draw, rounded corners, font=\small, align=center, minimum width=3.2cm, minimum height=10mm, fill=black!5}]
  \node[inst] (i0) {Instances 0--24\\\footnotesize Sponza static geometry};
  \node[inst, right=10mm of i0] (i1) {Instances 25--28\\\footnotesize four rotating objects};
  \begin{scope}[on background layer]
    \node[draw, dashed, rounded corners, fit=(i0)(i1), inner sep=7pt,
          label=above:{\small TLAS: 29 instances (rebuild on a ``dirty'' frame)}] (tlas) {};
  \end{scope}
  \node[blas, below=17mm of i0] (bA) {25 BLASes\\\footnotesize one per mesh};
  \node[blas, below=17mm of i1] (bB) {1 BLAS\\\footnotesize shared 4 times};
  \draw[->] (i0) -- (bA);
  \draw[->] ([xshift=-9mm]i1.south) -- (bB);
  \draw[->] ([xshift=-3mm]i1.south) -- (bB);
  \draw[->] ([xshift=3mm]i1.south) -- (bB);
  \draw[->] ([xshift=9mm]i1.south) -- (bB);
\end{tikzpicture}
\caption[Two-level acceleration structure (TLAS/BLAS)]{Two-level acceleration structure, with measured
numbers from the \texttt{Sponza-Motion.world} scene: the TLAS holds 29 instances (a transform plus a
pointer to a BLAS) over 26 BLASes, since four rotating objects share the same mesh while each of the
25 static meshes has its own. A BLAS holds geometry in local space.}
\label{fig:tlasblas}
\end{figure}

\section{Ray tracing in Vulkan: RT pipeline versus ray query}
\label{sec:vulkanrt}

Vulkan ray tracing rests on three extensions: \texttt{VK_KHR_acceleration_structure} to build and
manage the BVH, and two ways to trace against it, \texttt{VK_KHR_ray_tracing_pipeline} and
\texttt{VK_KHR_ray_query} \cite{khronos_rayquery}. The ray-tracing pipeline is the natural structure for a full path tracer:
hardware ray scheduling and recursion, at the price of dedicated stages (ray generation, closest-hit,
any-hit, miss, intersection) and a shader binding table, a GPU-side lookup that tells the hardware
which hit shader to run for each piece of geometry, and which must be built and kept in sync with the
scene. Inline ray queries
instead expose a \texttt{RayQuery} object usable from any
existing shader stage: the shader runs the traversal loop itself, with no binding table and no
recursion (Table~\ref{tab:pipeline-vs-rayquery}). This thesis uses ray queries because the effects
are bounded additions to an existing rasterizing renderer rather than a standalone tracer, as argued
in Section~\ref{sec:rendergraph}.

\begin{table}[tbp]
\caption[RT pipeline versus inline ray query]{Ray-tracing pipeline versus inline ray query in Vulkan.}
\label{tab:pipeline-vs-rayquery}
\centering
\begin{tabular}{lll}
\toprule
Aspect & RT pipeline & Ray query \\
\midrule
Shader stages        & dedicated (raygen, hit, miss) & any existing stage \\
Shader binding table & required & not required \\
Recursion            & supported & not supported \\
Hardware scheduling  & yes & no (the caller writes the loop) \\
Integration          & requires a separate pipeline & drop-in into an existing pass \\
Suited for           & full path tracer & bounded effects on a rasterizer \\
\bottomrule
\end{tabular}
\end{table}

Hardware scheduling means the pipeline, rather than the calling shader, decides when and in what
grouping hit shading runs. NVIDIA exposes an explicit form of this from the Ada generation onward as
shader execution reordering \cite{nvidia_ada}, where the shader hands traversal's result to the
driver and asks for invocations to be regrouped by hit before shading them. An inline query has
neither: the caller drives the loop and shades in place, so rays that diverge inside one wave run at
the pace of the slowest.

\section{Existing real-time solutions for GI and RT effects}
\label{sec:postojeca}

The effects implemented here have a long line of real-time approximations
(Table~\ref{tab:postojeca}), in two tiers. The screen-space family is implemented in this engine and
measured head to head against the ray-traced path in Chapter~\ref{ch:analiza}, while the voxel,
probe, and reservoir methods are surveyed for positioning only, neither implemented nor measured here, so no
quantitative claim is made about them.

The tiers fail differently. Screen-space methods fail on \emph{missing information}: they see only
what is currently on screen, so an off-screen occluder or reflector is simply absent, which
Figure~\ref{fig:ssvsrt-refl} shows directly, and which is the
class of error a ray query removes by consulting the real scene. Voxel and probe methods do see the
whole scene and fail instead on \emph{resolution}: their world representation is coarser than the
effect they reconstruct, which shows up as light leaking through thin walls or as smoothed-away
contact darkening. No amount of sampling fixes the first, but a finer grid, at a cost, mitigates the
second.

\begin{table}[tbp]
\caption[Existing approaches and their limits]{Families of existing solutions for the same effects.
The first three rows do not see the scene off screen, the next two see it but more coarsely than the
effect requires. The last two are complete systems rather than individual techniques.}
\label{tab:postojeca}
\centering
\small
\begin{tabularx}{\textwidth}{@{}l>{\raggedright\arraybackslash}X>{\raggedright\arraybackslash}X@{}}
\toprule
Approach & How it works & What it misses \\
\midrule
SSAO / HBAO \cite{mittring2007cryengine, bavoil2008hbao}
  & samples the depth buffer around the pixel, or integrates the horizon angle
  & everything off screen, plus thin geometry the depth buffer does not represent \\
\addlinespace[2pt]
SSR
  & marches the reflected ray through the depth buffer to an intersection
  & the same, plus disocclusion under camera motion \\
\addlinespace[2pt]
SSGI
  & marches over the hemisphere through the depth buffer and gathers the \emph{previous} frame's color
  & one bounce, from the screen only, lagging one frame and losing light behind the camera \\
\addlinespace[2pt]
Voxel GI \cite{crassin2011vxgi}
  & a voxel grid carrying radiance, cone traced through it
  & coarse resolution: light leaking through thin walls, memory and revoxelization \\
\addlinespace[2pt]
Probe / DDGI \cite{majercik2019ddgi, nvidia_rtxgi}
  & a grid of probes with irradiance and a visibility distribution
  & low frequency: loses contact darkening and fine indirect detail \\
\addlinespace[2pt]
Lumen \cite{epic_lumen}
  & software tracing of SDFs plus hardware tracing where available, with a radiance cache
  & a complete system, a reference point here rather than a target \\
\addlinespace[2pt]
ReSTIR \cite{bitterli2020restir, ouyang2021restirgi}
  & spatiotemporal reservoir resampling
  & state of the art, here future work (Chapter~\ref{ch:zakljucak}) \\
\bottomrule
\end{tabularx}
\end{table}

\section{Denoising for RT}
\label{sec:denoise-pregled}

The integral in Equation~\eqref{eq:rendering} has no closed form, so it is estimated by Monte Carlo
integration: drawing $N$ directions $\omega_k$ from a probability density $p$ and averaging,
\begin{equation}
L_o \approx L_e + \frac{1}{N}\sum_{k=1}^{N}
\frac{f_r(\omega_k)\, L_i(\omega_k)\, (\omega_k \cdot \mathbf{n})}{p(\omega_k)}.
\label{eq:mc}
\end{equation}
The estimator is unbiased, but its variance falls only as $1/N$, so the error falls as $1/\sqrt{N}$:
four times the rays halve the noise. At the one-to-few rays
per pixel a real-time budget allows, the raw estimate is heavily noisy, so denoising is a mandatory
part of the pipeline rather than an optional post-effect (Section~\ref{sec:raycount} measures how
little the extra rays buy once the denoiser runs). This thesis implements Spatiotemporal
Variance-Guided Filtering (SVGF) \cite{schied2017svgf}, which combines temporal reprojection and
accumulation with a variance-guided \`a-trous spatial filter whose blur strength adapts to the
estimated per-pixel variance, so it smooths noisy regions while preserving converged detail. The main
alternatives are its adaptive successor A-SVGF \cite{schied2018asvgf}, NVIDIA's production Real-Time
Denoisers (ReBLUR and ReLAX) \cite{nvidia_nrd}, and machine-learning denoisers such as Intel's Open
Image Denoise \cite{intel_oidn}. Over the denoised result, temporal anti-aliasing accumulates jittered
frames to remove aliasing and to converge the remaining noise \cite{karis2014taa}. Its interaction
with the per-effect denoisers is part of the implementation in Chapter~\ref{ch:implementacija}.
