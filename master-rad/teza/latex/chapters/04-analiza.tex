\chapter{Analiza}
\label{ch:analiza}

% Draft. Methodology (4.1) is complete and verified; result tables are placeholders to be filled
% from real runs. NO fabricated numbers.

This chapter evaluates the system. It first fixes the methodology, hardware, scene, and the two
measurement setups (Section~\ref{sec:metodologija}), then reports the per-effect GPU cost
(Section~\ref{sec:cena}), the ray-count versus quality and speed tradeoff
(Section~\ref{sec:raycount}), the contribution of the denoiser (Section~\ref{sec:denoise-eval}), a
comparison against the existing solutions from Chapter~\ref{ch:pregled} (Section~\ref{sec:poredjenje}),
and the static shader occupancy (Section~\ref{sec:occupancy}). Every number reported here is produced
by a measured run; cells still marked as pending have not yet been measured and are not estimated.

\section{Metodologija}
\label{sec:metodologija}

\paragraph{Hardver: cross-vendor postavka.} The central evaluation compares the identical pipeline on
two current-generation GPUs from different vendors: an AMD Radeon RX 9070 XT (RDNA4) and an NVIDIA
GeForce RTX 5070 (Blackwell). Both run the same engine binary, the same HLSL-compiled SPIR-V shaders,
the same render graph, and the same scene and settings; the only variable is the selected physical
device. That identity is the fairness basis of the comparison: any difference in timing is due to the
hardware and its driver, not to a different code path. Table~\ref{tab:gpus} lists the relevant
specifications. Because the algorithm is bit-for-bit identical on both GPUs, the quality metrics
(PSNR/SSIM) are expected to match across them; the cross-vendor result is therefore a performance
comparison, and any quality divergence would flag a non-determinism to investigate rather than a real
difference. The RX 7900 XTX (RDNA3) used during development serves as an optional generational
reference and as the target of the static occupancy analysis (Section~\ref{sec:occupancy}), which is
AMD-only.

\begin{table}[tbp]
\caption{GPU-ovi u cross-vendor pore\dj enju. Vrednosti se popunjavaju iz zvani\v{c}nih specifikacija.}
\label{tab:gpus}
\centering
\begin{tabular}{lll}
\toprule
 & AMD RX 9070 XT & NVIDIA RTX 5070 \\
\midrule
Arhitektura            & RDNA4 & Blackwell \\
Compute jedinice / SM  & \textit{--} & \textit{--} \\
RT akceleratori        & \textit{--} & \textit{--} \\
Takt (boost)           & \textit{--} & \textit{--} \\
Memorija / propusnost  & \textit{--} & \textit{--} \\
TDP                    & \textit{--} & \textit{--} \\
Drajver / OS           & \textit{--} & \textit{--} \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Scena.} The test scene is Sponza, a standard interior with strong indirect bounce, lit by
ten lights (three spot, six point, one directional) at a fixed render resolution stated with the
results. A code-generated stress scene (a grid of meshes) is available for scaling tests.

\paragraph{Controls for fair comparison.} Several controls keep the measurements honest and
reproducible. The camera is held static for every A/B comparison: a moving camera desynchronizes the
frame sequence between two runs and fabricates differences that are timing artifacts rather than real
effects. The shader cache is cleared before any GPU metric is trusted, since a stale cache produces
nondeterministic results. Performance baselines are per-machine and per-resolution, because absolute
milliseconds are not comparable across GPUs or output sizes. Persisted console variables are ignored
during a benchmark run so that a leftover setting cannot silently change the configuration.

\paragraph{Performance measurement.} Each render-graph pass is wrapped in a GPU timestamp scope, and
a harness (\texttt{perf-bench}) averages those per-pass timings over a fixed frame budget after a
fifteen-frame warmup that also covers the one-frame timestamp latency. The benchmark sweeps a fixed
matrix of configurations, \texttt{rt-off} $\rightarrow$ \texttt{shadows} $\rightarrow$ \texttt{+ao}
$\rightarrow$ \texttt{+refl} $\rightarrow$ \texttt{+gi}, enabling one more effect at each step, so the
cost of an effect is the difference between adjacent configurations. One caveat applies to that
difference: it equals the Forward-pass delta only for shadows and reflections, which are inline in
the Forward pass. Global illumination and ambient occlusion run as their own half-resolution passes
with separate timestamp scopes, so their cost is read from those per-pass scopes plus the total GPU
time, not from the Forward delta alone. Across vendors, the raw timestamps are converted to
milliseconds using each device's timestamp period, so the per-pass times are directly comparable;
results are reported for both GPUs side by side rather than diffed against a single-machine baseline,
since here the cross-vendor comparison is itself the measurement.

\paragraph{Quality measurement.} Reconstruction quality is measured in-engine by a metrics pass that
computes PSNR and SSIM on the GPU against a ground-truth image. The ground truth is a second,
unjittered, full-resolution forward render of the same frame, optionally supersampled
(\texttt{render.gt.ssaa}) and box-downsampled in linear HDR to give an anti-aliased reference. For
the reconstruction-quality comparison the reference is rendered with denoising disabled and a high
supersampling factor so that it approximates a converged image; the factor used and the resulting
effective sample count are stated with the results. A
second caveat applies here: the ground-truth path forces raster shadows, so with ray-traced shadows
enabled the metric compares ray-traced against raster shadows, not against a ray-traced reference.
Shadow-quality numbers are therefore either measured on the non-shadow effects against the reference,
or reported with this caveat stated explicitly.

\section{Cena po efektu (GPU ms)}
\label{sec:cena}

The per-effect cost is the adjacent-configuration difference from the benchmark matrix, read from the
per-pass timestamp scopes as described above, and reported for both GPUs so the cross-vendor
difference per effect is explicit. Table~\ref{tab:cena} reports it.

\begin{table}[tbp]
\caption{Cena po efektu, cross-vendor (GPU ms). Vrednosti se popunjavaju iz stvarnog merenja.}
\label{tab:cena}
\centering
\begin{tabular}{llll}
\toprule
Konfiguracija & Efekat & ms (9070 XT) & ms (5070) \\
\midrule
\texttt{rt-off}   & bazna linija          & \textit{--} & \textit{--} \\
\texttt{+shadows} & senke                 & \textit{--} & \textit{--} \\
\texttt{+ao}      & ambijentalna okluzija & \textit{--} & \textit{--} \\
\texttt{+refl}    & refleksije            & \textit{--} & \textit{--} \\
\texttt{+gi}      & globalno osvetljenje  & \textit{--} & \textit{--} \\
\bottomrule
\end{tabular}
\end{table}

Figure~\ref{fig:cost-chart} presents the same data graphically once measured.

\begin{figure}[tbp]
\centering
\begin{tikzpicture}
\begin{axis}[ybar, bar width=9pt, width=0.9\textwidth, height=6cm,
  symbolic x coords={senke,AO,refl,GI}, xtick=data,
  ymin=0, ylabel={GPU ms}, xlabel={efekat},
  legend style={at={(0.5,1.04)}, anchor=south, legend columns=-1},
  enlarge x limits=0.25]
\addplot table[x=effect, y=ms9070] {data/perf-cost.dat};
\addplot table[x=effect, y=ms5070] {data/perf-cost.dat};
\legend{AMD RX 9070 XT, NVIDIA RTX 5070}
\end{axis}
\end{tikzpicture}
\caption{Cena po efektu, cross-vendor (GPU ms). Prazan grafik (nulte vrednosti); popunjava se iz
\texttt{data/perf-cost.dat} nakon merenja.}
\label{fig:cost-chart}
\end{figure}

\textit{TODO: fill from perf-bench runs on both GPUs; comment on which effect dominates on each and
where the vendors diverge (expected: GI and shadows are the largest costs; the half-res + denoise
pattern is what keeps GI and AO affordable; RT-hardware and driver differences show up most in the
trace-heavy passes).}

\begin{figure}[tbp]
\centering
\phshot{0.46\textwidth}{3.8cm}{senke}\hfill\phshot{0.46\textwidth}{3.8cm}{ambijentalna okluzija}

\vspace{5pt}

\phshot{0.46\textwidth}{3.8cm}{refleksije}\hfill\phshot{0.46\textwidth}{3.8cm}{globalno osvetljenje}
\caption{Pojedina\v{c}ni efekti izolovani na sceni Sponza. \textit{(placeholderi; zameniti stvarnim
snimcima.)}}
\label{fig:effect-gallery}
\end{figure}

\section{Broj zrakova naspram kvaliteta i brzine}
\label{sec:raycount}

This section sweeps the samples-per-pixel count for each effect (and, where relevant, ray length and
bounce count), plotting reconstruction quality (PSNR/SSIM) and GPU time against the ray count, to
find the knee past which additional rays stop paying for themselves. \textit{TODO: run the sweep and
present the plots; this is the central quality/performance tradeoff the research question asks about.}

\section{Evaluacija denoiser-a (SVGF)}
\label{sec:denoise-eval}

This section isolates the denoiser's contribution: the noisy input (denoiser disabled) against the
denoised result against the reference, at a fixed low sample count, reporting the PSNR/SSIM gain, the
temporal stability under motion, and the denoiser's own GPU cost against the quality it recovers.
\textit{TODO: measure denoiser on/off; tie the cost to the register-limited \texttt{GIDenoise.comp}
shader from Section~\ref{sec:denoising}.}

\begin{figure}[tbp]
\centering
\phshot{0.30\textwidth}{4cm}{\v{s}um (1--2 spp)}\hfill%
\phshot{0.30\textwidth}{4cm}{denoise-ovano}\hfill%
\phshot{0.30\textwidth}{4cm}{referenca (SSAA)}
\caption{Doprinos denoiser-a: \v{s}um pri niskom broju uzoraka, denoise-ovan rezultat, i referenca.
\textit{(placeholderi; zameniti stvarnim snimcima.)}}
\label{fig:denoise-compare}
\end{figure}

\section{Pore\dj enje sa postoje\'cim re\v{s}enjima}
\label{sec:poredjenje}

This section positions the ray-traced effects against the existing solutions surveyed in
Chapter~\ref{ch:pregled}. The engine does not implement screen-space baselines (SSAO, SSR, SSGI), so
the comparison against them is qualitative: it points, on ray-traced captures, to the cases those
methods structurally cannot handle (off-screen occluders and reflectors, disocclusion) and relates
them to the limitations set out in Section~\ref{sec:postojeca}, rather than an in-engine A/B. The
quantitative comparison is against the converged reference of Section~\ref{sec:metodologija}: how
close the real-time denoised hybrid comes in PSNR/SSIM and where it visibly diverges
(Figure~\ref{fig:rt-vs-ref}). The positioning against production systems (Lumen, RTXGI, ReSTIR) is
qualitative: the contribution is an integrated, measured, openly licensed ray-query implementation,
not a system that beats a production global-illumination solution. \textit{TODO: produce the
comparison captures and the reference quality gap.}

\begin{figure}[tbp]
\centering
\phshot{0.46\textwidth}{4.5cm}{real-time (ovaj rad, nizak spp + denoise)}\hfill%
\phshot{0.46\textwidth}{4.5cm}{referenca (SSAA, konvergirano)}
\caption{Real-time hibridni izlaz naspram konvergirane reference (SSAA). \textit{(placeholderi;
zameniti stvarnim snimcima.)}}
\label{fig:rt-vs-ref}
\end{figure}

\section{Zauzetost i cena \v{s}ejdera (opciono)}
\label{sec:occupancy}

This section reports the static shader occupancy (VGPR/LDS usage and register spills) per ray-tracing
and denoising shader, from the offline occupancy gate, and identifies the one occupancy-limited
shader (\texttt{GIDenoise.comp}). These figures are static and theoretical (register-derived
occupancy), distinct from achieved occupancy measured with a runtime profiler. \textit{TODO: include
the occupancy table.}
